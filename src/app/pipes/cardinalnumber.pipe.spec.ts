import { CardinalnumberPipe } from './cardinalnumber.pipe';

describe('CardinalnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new CardinalnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
